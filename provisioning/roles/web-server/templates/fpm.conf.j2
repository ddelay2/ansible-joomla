[{{ fpm_pool_name }}]

user = {{ fpm_pool_user }}
group = {{ fpm_pool_user }}

listen = /run/php/{{ fpm_pool_name }}.sock

listen.owner = {{ fpm_pool_user }}
listen.group = nginx
listen.mode = 0660

pm = static
pm.max_children = 2
pm.max_requests = 500
